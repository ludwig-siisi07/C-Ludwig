#include<stdio.h>
int main()
{
    //arithmetic operations (+,*,-,%,/)
    int x = 5;
    int y = 3;
    int z = 0; //z stores the result
    int z2 = 0;
    
    //increments and decrements(useful for loops)
    x++;
    y--;
    printf("%d\n",x);
    printf("%d\n",y);

    return 0;
}
