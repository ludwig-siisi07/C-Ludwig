#include<stdio.h>
int main()
{
    //variable = a reusable container
    /*Strings = array of characters
    uses double quotes
    format specifier - %s
    */
   char name [] = "Ludwig Siisi";
   printf("Hello, %s!\n",name);

    return 0;
}