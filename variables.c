#include<stdio.h>
int main()
{
    //variable = a reusable container
    float gpa = 3.7;
    //floats use the format specifier %f and usually have 6 digits after decimal
    printf("Your gpa is %.2f\n",gpa);
    printf("Excellent!\n");

    return 0;
}